// Sliding Window

let left = 0;

for (let right = 0; right < arr.length; right++){
    // expand the window

    while(window violates condition) {
        // shrink window from left

        left++
    }

    // track max/min/etc..
}

/*  ------------------------------------------------------------

    When to Use:
    - Problems asking for subarrays, substrings, or ranges
    - Fixed size(like "max sum f k-length subarray")
    - variable size (like "longest substring with X condition")

    Practice Problems:
    - Maximum Sum of Subarray of Size K (fixed)
    - Longest Substring Without Repeating Characters (variable)
    - Permutation in String

*/