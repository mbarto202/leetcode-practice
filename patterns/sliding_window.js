/* When to Use:
    - Problems asking for subarrays, substrings, or ranges
    - Fixed size(like "max sum f k-length subarray")
    - variable size (like "longest substring with X condition")
*/

// Sliding Window Pattern
